import styled from 'styled-components';

const GenericInnerContainer = styled.div`
  width: 100%;
  max-width: var(--container-width);
  padding: var(--container-padding);
  margin: 0 auto;
`;

const GenericSection = styled.section`
  width: 100%;
  display: inline-grid;
`;
